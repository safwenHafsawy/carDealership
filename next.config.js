/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "dummyimage.com",
      "images.unsplash.com",
      "*.googleusercontent.com",
    ],
  },
};

module.exports = nextConfig;
